fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
python-multipart>=0.0.9

# auth-foundation
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
asyncmy>=0.2.9
aiomysql>=0.2.0
redis>=5.0
passlib[bcrypt]>=1.7
# bcrypt 5.0+ 与 passlib 1.7.x 不兼容（passlib 内部探测 backend 时会因严格的 72-byte 限制崩溃）
bcrypt<5
PyJWT>=2.8
pydantic-settings>=2.0
email-validator>=2.0

# 限流：slowapi 是 starlette 友好的 RateLimiter，支持装饰器 + Redis backend
slowapi>=0.1.9

# api_key 加密存储（Fernet 对称加密）
cryptography>=42.0
